# Required because @stripe/react-stripe-js peer dependency declares React ^16-18
# but FundTrust uses React 19. The library works correctly at runtime with React 19 —
# this flag just tells npm to skip the strict peer version check during install.
legacy-peer-deps=true